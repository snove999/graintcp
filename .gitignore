# 混淆/测试工具链（本地）
node_modules/
package.json
package-lock.json

# 本地开发与回归产物
_worker_test.mjs
*.tmp

# 上游参考快照（仅本地对比用，不入库）
_ref_edgetunnel.tmp

# 混淆产物验证临时目录
_verify/

# 内部补丁脚本
_surgery_*.cjs
_worker_*_patch.cjs
_obfuscate.mjs

# 独立子项目（另行管理）
pages-sub-page/
